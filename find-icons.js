const si = require('react-icons/si');
console.log('AfterEffects:', Object.keys(si).filter(k => k.toLowerCase().includes('after')));
console.log('Adobe:', Object.keys(si).filter(k => k.toLowerCase().includes('adobe')));
